🔹 JavaScript Methods Cheat Sheet
1️⃣ String Methods
Method	কাজ
length	String-এর দৈর্ঘ্য (character সংখ্যা) বের করে
toUpperCase()	সব অক্ষরকে বড় হাতের অক্ষরে রূপান্তর করে
toLowerCase()	সব অক্ষরকে ছোট হাতের অক্ষরে রূপান্তর করে
trim()	String-এর শুরু ও শেষে থাকা space কেটে দেয়
slice(start, end)	নির্দিষ্ট অংশ কেটে আনে
substring(start, end)	অংশ বের করে আনে (negative index নেয় না)
substr(start, length)	start থেকে length সংখ্যক অংশ আনে
includes("txt")	String-এর মধ্যে txt আছে কিনা চেক করে
indexOf("txt")	txt কোথায় প্রথম পাওয়া গেছে তার index দেয়
lastIndexOf("txt")	txt কোথায় শেষবার পাওয়া গেছে তার index দেয়
replace("old","new")	String-এর মধ্যে old কে new দিয়ে replace করে
split("separator")	String কে array তে ভাগ করে
concat()	একাধিক string জোড়া লাগায়
2️⃣ Array Methods
Method	কাজ
push()	array-র শেষে নতুন element যোগ করে
pop()	array-র শেষ element মুছে ফেলে
shift()	প্রথম element মুছে ফেলে
unshift()	প্রথমে নতুন element যোগ করে
slice(start,end)	নতুন array তৈরি করে নির্দিষ্ট অংশ কেটে আনে
splice(start, count, newItems)	array থেকে অংশ বাদ দেয় বা নতুন element যোগ করে
concat()	দুই বা ততোধিক array জোড়া লাগায়
join("separator")	array কে string বানায়
indexOf(item)	element-এর index বের করে
includes(item)	element আছে কিনা true/false দেয়
forEach(fn)	প্রতিটি element এর জন্য কাজ চালায়
map(fn)	প্রতিটি element পরিবর্তন করে নতুন array তৈরি করে
filter(fn)	শর্ত মেনে যেগুলো true সেগুলো রাখে
find(fn)	প্রথম যে element শর্ত পূরণ করে সেটি দেয়
findIndex(fn)	প্রথম যে element শর্ত পূরণ করে তার index দেয়
reduce(fn, init)	সব element যোগ বা process করে একক মান দেয়
sort()	array sort করে (string হিসেবে)
reverse()	array উল্টে দেয়
3️⃣ Math Methods
Method	কাজ
Math.round(x)	সংখ্যাকে নিকটবর্তী পূর্ণসংখ্যায় নেয়
Math.ceil(x)	সবসময় উপরের দিকে পূর্ণসংখ্যায় নেয়
Math.floor(x)	সবসময় নিচের দিকে পূর্ণসংখ্যায় নেয়
Math.random()	0 থেকে 1 এর মধ্যে random সংখ্যা দেয়
Math.max(a,b,...)	সর্বোচ্চ সংখ্যা বের করে
Math.min(a,b,...)	সর্বনিম্ন সংখ্যা বের করে
Math.sqrt(x)	বর্গমূল বের করে
Math.pow(x,y)	x^y (ঘাত) হিসাব করে
4️⃣ Date Methods
Method	কাজ
new Date()	বর্তমান তারিখ ও সময় দেয়
getFullYear()	বছর বের করে
getMonth()	মাস বের করে (0-11, জানুয়ারি=0)
getDate()	মাসের তারিখ দেয়
getDay()	সপ্তাহের দিন দেয় (0=Sunday)
getHours()	ঘন্টা বের করে
getMinutes()	মিনিট বের করে
getSeconds()	সেকেন্ড বের করে
setFullYear(year)	বছর সেট করে
setMonth(month)	মাস সেট করে
setDate(day)	তারিখ সেট করে
5️⃣ Object Methods
Method	কাজ
Object.keys(obj)	object-এর সব key array আকারে দেয়
Object.values(obj)	object-এর সব value array আকারে দেয়
Object.entries(obj)	key-value pair এর 2D array দেয়
Object.assign(target, source)	এক object এর data অন্যটাতে কপি করে
Object.freeze(obj)	object lock করে, নতুন property যোগ/পরিবর্তন করা যায় না
Object.seal(obj)	নতুন property যোগ করা যাবে না, তবে পুরোনো value বদলানো যাবে
6️⃣ JSON Methods
Method	কাজ
JSON.stringify(obj)	object → JSON string এ রূপান্তর করে
JSON.parse(string)	JSON string → object এ রূপান্তর করে